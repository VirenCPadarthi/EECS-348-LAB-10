#ifndef CALCULATOR_HPP
#define CALCULATOR_HPP

#include <string>

bool isValidDouble(const std::string& s);
std::string addStrings(const std::string& s1, const std::string& s2);
void normalizeStrings(std::string& a, std::string& b);

#endif
